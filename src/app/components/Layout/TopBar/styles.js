import { colors } from '../../../../theme';

const styles = {
  appBar: {
    position: 'fixed',
    top: 0,
    padding: '0 24px'
  },
  badge: {
    backgroundColor: colors.danger
  },
  iconStyleRight: {
    padding: 0
  }
};

export default styles;
