import { colors } from '../../../../../theme';

const styles = {
  toolbar: {
    marginBottom: 32
  },
  textField: {
    width: '100%'
  },
  selectField: {
    width: '100%'
  },
  activeViewMode: {
    color: colors.secondary
  }
};

export default styles;
